---
---

<div class="py-8">
  <p class="text-sm text-gray-600 text-center mb-6">Selected partners (permissions in progress)</p>
  <div class="grid grid-cols-2 md:grid-cols-5 gap-6">
    {Array.from({ length: 10 }).map((_, i) => (
      <div class="aspect-[3/2] bg-gray-50 rounded-lg border border-gray-200 flex items-center justify-center">
        <div class="w-16 h-16 bg-gray-200 rounded-full opacity-30" />
      </div>
    ))}
  </div>
</div>
